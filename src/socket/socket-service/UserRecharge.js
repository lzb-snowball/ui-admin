import UserRechargeBase from '@/parent-ui/src/main/business/common/socket/socket-service/UserRecharge'

class UserRecharge extends UserRechargeBase {
  // doUpdate = (dataEntity, opt, data, store) => {
  //  alert('platform/UserRecharge')
  // }
}

export const instance = new UserRecharge()
